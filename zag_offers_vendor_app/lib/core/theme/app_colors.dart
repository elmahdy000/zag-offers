import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF1E293B); // Dark slate for vendors
  static const Color primaryLight = Color(0xFF334155);
  static const Color secondary = Color(0xFF3B82F6); // Trust blue
  static const Color accent = Color(0xFFF59E0B); // Amber for alerts/actions
  static const Color background = Color(0xFFF8FAFC);
  static const Color card = Colors.white;
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
}
